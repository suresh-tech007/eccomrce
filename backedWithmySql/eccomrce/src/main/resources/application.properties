# Application Name
spring.application.name=eccomrce

# Server Port Configuration
server.port=8080 

# Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:eccomrce}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:sureshKU@120}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate & JPA Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Debugging Configuration
debug=false

# Razorpay API Configuration
razorpay.api.key=${RAZORAPIKEY:rzp_test_PReClXvE4O3upL}
razorpay.api.secret=${RAZORSECRET:hO1ZUu5vS8lfJXF9faWTkSaX}
